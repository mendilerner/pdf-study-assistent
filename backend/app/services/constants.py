# Shared across chunker.py (tokenizer) and embeddings.py (model).
# Both must use the exact same model so token counts match vector dimensions.
MODEL_NAME = "intfloat/multilingual-e5-base"
EMBEDDING_DIMS = 768
